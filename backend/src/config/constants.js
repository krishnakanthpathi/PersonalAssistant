/**
 * Platform keys, paths, and model configurations
 */
export const PLATFORM_KEYS = {
	// Define API keys / secrets here
};

export const MODEL_CONFIG = {
	defaultModel: 'llama3',
	temperature: 0.7,
};
